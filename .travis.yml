language: python
python:
  - "3.6"
  - "3.7"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
# command to run tests
script:
  - pytest --cov=segcheck tests/
after_success:
  - coveralls